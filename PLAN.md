# B站抽奖雷达（BiliRadar）· 1.0 产品计划书

> 状态：v1.1（已综合外部评审意见，随开发持续修订）
> 最近更新：2026-09-01

---

## 一、项目概述

**一句话定位**：B站抽奖雷达——一个只读的自动化工具，监控 B 站抽奖信息，按门槛过滤后把「奖品 / 要求 / 开奖时间 / 链接」推送到你的 Telegram。

**背景动机**：B站大量博主动态抽奖，仅靠人眼盯关注列表会漏掉海量「碰运气薅羊毛」的机会。项目目标是扩大抽奖信息的覆盖分母，并自动过滤低质 / 诈骗抽奖。

**项目性质**：个人自用工具；好用后再评估适度开源。

---

## 二、目标与边界

### 目标
- 覆盖尽量广的 B 站抽奖信息来源（不局限于自己关注的 UP 主）
- 自动提取关键信息：奖品、参与要求、开奖时间、原链接
- 按粉丝量门槛 + 风险信号过滤，降低诈骗中招概率
- 推送渠道免费、无每日配额、体验好
- 系统自身可靠：出问题（Cookie 失效等）能被主动告警，而非静默失败

### 明确不做（边界）

| 不做 | 原因 |
|---|---|
| 自动参与抽奖（转发 / 评论 / 关注） | 违反平台协议、封号风险高，违背「只读」安全底线 |
| 多平台（抖音 / 微博等） | 一期聚焦 B 站；抖音接口复杂，留待评估 |
| GUI / Web 管理界面 | 个人自用，CLI + 配置文件即可；2.0 再议 |
| 中奖跟进 / 开奖提醒 | 列入 2.0 规划 |

---

## 三、产品架构（三源漏斗）

```
[cron / 青龙 定时触发]
        ↓
[采集层] 话题矩阵 + 合集UP主镜像 + (可选)关注列表 → 原始动态（polymer 新 API）
        ↓
[去重层] 按 dynamic_id 去重（SQLite，30 天 TTL 清理）
        ↓
[识别层] 官方「互动抽奖」组件结构化解析优先 → 无组件时降级文本规则引擎
        ↓
[过滤层] 粉丝量门槛 / UP主UID黑名单 / 关键词黑白名单 / 高风险一票否决 / 复杂度过滤 / 官方优先
        ↓
[推送层] Telegram 双通道：抽奖推送 + 系统告警（Alert）
        ↓
[存储] 已推送记录 + 产品日志（LOG.md）+ 运行日志（runtime.log）
```

### 三源说明
- **源1 · 话题矩阵（主力）**：监控多个抽奖相关话题页，覆盖大量不认识的博主的抽奖 → 扩大分母（S0 已验证：`feed/topic` 免登录可用）
- **源2 · 合集UP主镜像（聪明钱）**：监控专门发「抽奖合集」的 UP 主，人工聚合即现成全网雷达，与源1互补助漏。**S0 发现其价值被低估**：合集文章本身就是"人工审核的结构化抽奖列表"（开奖时间 + 奖品 + UP主uid + 链接），比话题 feed 原始动态更干净，1.0 应重点利用
- **源3 · 关注列表（可选开关）**：个人补充，非主力（需 wbi 签名，S2 处理）

---

## 四、功能模块

| 模块 | 要点 |
|---|---|
| 采集层 | 对齐 **polymer 新 API**（`api.bilibili.com/x/polymer/web-dynamic/v1/feed/all`，话题 / 空间 feed，避开已废弃的旧版 `api.vc.bilibili.com`）；礼貌限速 + 随机延迟，降低风控 |
| 去重层 | 按 `dynamic_id` 去重；**TTL 清理**：每次运行后删除 30 天前记录，防表无限膨胀、查询变慢 |
| 识别层 | **官方「互动抽奖」组件结构化解析优先**（S0 实测确认：当前 API 的 `additional` 字段已不再返回抽奖组件，官方抽奖以**富文本节点 `RICH_TEXT_NODE_TYPE_LOTTERY`** 内嵌在 `major.opus.summary.rich_text_nodes` / `desc` 中，节点含抽奖 **`rid`**；奖品 / 参与条件在正文文本中提取，开奖时间接口待 S1 挖取）；无官方组件时**降级文本规则引擎**（抽奖 / 转发 / 三连 / 评论抽奖等关键词 + 条件解析（关注 / 转发 / @好友 / 带话题）+ 时间提取（"开奖时间/开奖日期"正则））。**需区分「单条抽奖」与「合集 / 汇总帖」**（S0 实测发现规则会误判汇总帖为单条抽奖，通过命中词密度 / 是否含多条"开奖日期+奖品+要求"块 / 标题含"合集"特征来判别） |
| 过滤层 | **粉丝量门槛（必选）** + **UP主UID黑名单**（惯犯直接丢弃）+ 关键词黑白名单 + **高风险「一票否决」**（抽奖+充电 / 橱窗 / 购买等诱导消费→直接拦截）+ 复杂度过滤 + 风险信号标记 + 官方优先 |
| 推送层 | Telegram **双通道**：①抽奖推送（含 **Deep Link `bilibili://`** 一键唤起APP直达 + 内联 **URL 按钮「直达B站」**）；②**系统告警**：Cookie 失效 / 鉴权失败时推 `[Alert]` 到 TG，杜绝静默失败。Callback「已参与」按钮列为增强（见五） |
| 存储层 | SQLite（已推送记录）+ 产品日志（LOG.md，时间精确到分钟 + 全局序号）+ 运行日志（runtime.log，程序报错 / 请求级） |

---

## 五、1.0 范围与里程碑

| 切片 | 内容 | 验收标准 | 里程碑 |
|---|---|---|---|
| S0 | 单话题抓取（polymer API）+ 识别（官方组件优先 / 文本降级）+ 终端打印 | 终端可见「发现 N 个抽奖：奖品 / 要求 / 链接」 | M1-M2 |
| S1 | + Telegram 推送（含 Alert 告警、Deep Link、URL 按钮）+ 去重 | 手机收到第一条推送；Cookie 失效能收到 `[Alert]` | M2（**已完成**：多条真实推送已发出；Alert 通道已建） |
| S2 | + 多话题矩阵 + 合集UP主 + 粉丝量过滤 + UID黑名单 + 一票否决 | 推送量与质量明显提升 | M3（**已完成**：话题12031+合集UP/文章清单、粉丝过滤、UID黑名单18个、一票否决均落地并上线） |
| S3 | 风险信号、黑白名单、配置化、日志（含 runtime.log）、TTL 清理 | 可日常使用 | M3（**基本完成**：公示帖拦截/黑白名单/配置化/日志/TTL均落地；识别精度已验证） |

---

## 六、技术方案

- **语言 / 运行时**：Node.js（可借鉴 LotteryAutoScript 的监控 / 推送逻辑，验证过、省逆向成本）
- **推送**：Telegram Bot API（免费、无每日配额，约 1 条/秒宽松频率限制）
- **存储**：SQLite（去重表 + TTL 清理）/ JSON 配置
- **调度**：cron / 青龙面板（跑完即走，不搞常驻服务；若启用 Callback「已参与」按钮则需额外常驻长轮询服务，纳入 1.5 评估）
- **部署**：优先本地 / 家用宽带 IP（NAS、旧手机、常开电脑），规避机房 IP 画像风控
- **网络代理**：**Telegram API 需走本地代理**（实测直连 `api.telegram.org` 超时，`127.0.0.1:7897` Clash 系可用）；脚本需支持 `HTTP_PROXY` 环境变量，B站接口则走直连（免登录话题feed已验证）
- **授权模型**：个人小号 Cookie（只读不互动，隔离账号风险）
- **许可提醒**：LotteryAutoScript 为 GPL-3.0，借鉴需同源开源；若想 MIT 则只参考思路自行实现

---

## 七、风险与合规

| 风险 | 说明 | 对策 |
|---|---|---|
| 平台协议 / 风控 | B站禁止自动化干扰运营，脚本有封号 / 限制抽奖权限案例 | 只读不写行为，风险等级低；礼貌限速 + 随机延迟；不做激进反指纹，请求头匹配真实浏览器 UA 即可 |
| IP 画像 | 机房 IP（云服务器）易触发 412 / 极验验证码 | 本地 / 家用 IP 部署优先；避免部署在阿里云 / 腾讯云等机房 |
| Cookie 安全 | 泄露或过期有账号风险 | 专用小号、只读、环境变量管理、不提交仓库；**失效时 TG 主动告警** |
| GPL 许可 | 借鉴代码需同源开源 | 明确借鉴边界并记录 |
| 个人数据 | 处理抽奖动态 / 用户信息 | 仅本地存储、最小化收集 |

---

## 八、待定事项（Open Questions）

- [x] 话题 feed 接口可用性 —— **S0 已验证**：`feed/topic` 免登录、curl 直连可用，返回 `topic_card_list.items[].dynamic_card_item`；S0 脚本独立运行命中真实抽奖
- [x] polymer 新 API 返回结构 —— **S0 已确认**：动态卡片含 `modules.module_author / module_dynamic(desc, major, additional, topic) / module_stat`；**`additional` 已不再返回抽奖组件**，官方抽奖在富文本节点 `RICH_TEXT_NODE_TYPE_LOTTERY`（含 rid）
- [ ] 实测 B 站抽奖相关话题清单 —— 需确认"互动抽奖"等话题的 `topic_id`（S1 用登录态/话题页挖取）
- [ ] 官方「互动抽奖」的**开奖时间**获取接口 —— S0 已确认检测与奖品提取，但按 rid 查开奖时间的接口未暴露（常见端点均 404）；回退方案：合集文章自带开奖时间 / 文本正则"开奖时间/开奖日期"
- [ ] 空间 feed（源3 关注列表 / 源2 空间）—— 需 wbi 签名，S2 处理
- [x] 确认 Telegram bot token / chat_id —— **已完成**：@&lt;BotName&gt;（token 有效，测试推送成功）；chat_id=user_id 私聊可用；**Telegram 需走本地代理 127.0.0.1:7897**
- [x] **S1 数据源** —— 种子清单（已验证真实抽奖 id）+ 话题 feed + **评论置顶传送门发现机制**（top_replies 免登录可读，含每日 cv 文章链接）均已实现；合集文章全文接口在沙箱 IP 被限流（-509），待家用 IP 部署验证
- [x] 开奖时间提取 —— 已修复正则（支持"2026年9月4日""4.30"等），过期判断按当前年
- [ ] 官方「互动抽奖」的**开奖时间**获取接口 —— 官方抽奖动态正文不含开奖时间（在 lottery 组件/合集文章里）；待挖 lottery 接口或依赖合集文章
- [x] `bilibili://` Deep Link 动态 URI 格式验证（S1）—— **结论：维持 https**。B站 scheme 仅有 video/article/space 权威格式，动态/opus 无权威格式；https URL 按钮经 Universal Links 在 TG 内点击可唤起 APP，稳定不切换
- [x] Cookie 失效 / 鉴权失败的接口返回信号确认（Alert 触发条件，S1）—— **已完成**：`checkLogin`（x/web-interface/nav）主流程前置检查，失效/未登录 → TG Alert（同日去重），不阻断主流程（话题源免登录可跑）
- [ ] 频道推送（可选）：若推送到频道而非私聊，需把 bot 设为频道管理员并提供频道 chat_id（当前用 user_id 私聊即可）
- [ ] Inline Callback「已参与」是否纳入（需常驻 Bot 服务，1.0 后评估）
- [x] 确定「合集UP主」候选名单（已有：你的抽奖工具人 mid=100680137；S0 从合集文章提取到 12dora/碧蓝航线/陈墨啥都玩/1818黄金眼等抽奖UP主）+ **UP 主 UID 黑名单初始名单（已完成 18 个：来源合集UP主【假抽奖十八宗罪】曝光栏 cv42547576，2025-08，含精准抽奖/左手倒右手/假大奖骗粉/抽奖要充电/开奖不公开等手法；一方称曝光，可自行增删）**
- [x] 抽奖识别规则精度验证（误报 / 漏报）—— **已完成**：话题最新20条 官方1+文本10+未识别9；修复 1 误报（**开奖公示帖漏拦**，applyFilters 加 `开奖公示/开奖结果公布/中奖名单/开奖名单/本次抽奖已[经于]?` 拦截）；漏报存疑（空格变形"粉 丝 福 利"、约稿粉福类）判可接受

---

## 九、参考资源

- [shanmiteko/LotteryAutoScript](https://github.com/shanmiteko/LotteryAutoScript)（GPL-3.0）—— 监控 / 推送逻辑参考
- [wumengwei1/Bili-Lottery-Clicker](https://github.com/wumengwei1/Bili-Lottery-Clicker)（MIT）—— 抽奖链接识别参考
- [ItsTimeTooSleep/Ding-Prize](https://github.com/ItsTimeTooSleep/Ding-Prize---Your-Bilibili-Lottery-Notifier)（MIT）—— 推送提醒参考
